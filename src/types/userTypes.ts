export interface User {
    username: string;
    email: string;
    dob: string; // Assuming this is a date in string format
    street: string;
    city: string;
    state: string;
    zip: string;
  };